services:
  playwright-app-1:
    build:
      context: ./playwright
      dockerfile: Dockerfile
    command: sh -c "yarn start"
    ports:
      - 127.0.0.1:3002:3000
    working_dir: /app
    # volumes:
    #   - ./:/app
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: root
      MYSQL_PASSWORD: secret
      MYSQL_DB: headlines
      MYSQL_TABLE: Another_Debate_2024
      SEARCH_TERM: trump harris second debate
      DATA_TOKEN: 0x78234aafc88ab34b177109a5448cd90d7d1fd4efea1983d19fd67eee78ff0239

  playwright-app-2:
    build:
      context: ./playwright
      dockerfile: Dockerfile
    command: sh -c "yarn start"
    ports:
      - 127.0.0.1:3001:3000
    working_dir: /app
    # volumes:
    #   - ./:/app
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: root
      MYSQL_PASSWORD: secret
      MYSQL_DB: headlines
      MYSQL_TABLE: Israel_Iran_Response
      SEARCH_TERM: israel iran response
      DATA_TOKEN: 0x324406e91743e6fe0f42221b872e4d8ee7279d876af357f3403a30e622f73195

  proxy:
    image: traefik:v2.11
    command: --providers.docker
    ports:
      - 80:80
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
  backend:
    build:
      context: ./dashboard
      target: backend-dev
    environment:
      MYSQL_HOST: mysql
      MYSQL_USER: root
      MYSQL_PASSWORD: secret
      MYSQL_DB: headlines
    develop:
      watch:
        - path: ./dashboard/backend/src
          action: sync
          target: /usr/local/app/src
        - path: ./dashboard/backend/package.json
          action: rebuild
    labels:
      traefik.http.routers.backend.rule: Host(`localhost`) && PathPrefix(`/api`)
      traefik.http.services.backend.loadbalancer.server.port: 3000   
  client:
    build:
      context: ./dashboard
      target: client-dev
    develop:
      watch:
        - path: ./dashboard/client/src
          action: sync
          target: /usr/local/app/src
        - path: ./dashboard/client/package.json
          action: rebuild
    labels:
      traefik.http.routers.client.rule: Host(`localhost`)
      traefik.http.services.client.loadbalancer.server.port: 5173
  phpmyadmin:
    image: phpmyadmin
    environment:
      PMA_HOST: mysql
      PMA_USER: root
      PMA_PASSWORD: secret
    labels:
      traefik.http.routers.phpmyadmin.rule: Host(`db.localhost`)
      traefik.http.services.phpmyadmin.loadbalancer.server.port: 80
  mysql:
    image: mysql:8.0
    volumes:
      - todo-mysql-data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: headlines

volumes:
  todo-mysql-data:
